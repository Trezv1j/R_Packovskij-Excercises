// Speed in kilometers per hour
let speed = 60;

// Distance traveled in meters
let distance = 264;

// Calculating the time to pass through a tunnel at the given speed
let tunnelTime = distance / (speed / 3.6); // Converting speed from km/h to m/s

// Displaying the time to pass through the tunnel
console.log(tunnelTime);